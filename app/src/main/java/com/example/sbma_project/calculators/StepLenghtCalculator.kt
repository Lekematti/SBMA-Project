/*package com.example.sbma_project.calculators

import com.example.sbma_project.repository.RunViewModel

class StepLengthCalculator(runViewModel: RunViewModel) {
    private val userHeight = runViewModel.getUserHeight()
    private var stepLength = userHeight * 0.415 // Adjust the multiplier as needed


    fun calculateAverageStepLength(): Double {
        val steps = StepCounter.getStepCount()
        return if (steps != 0) stepLength / steps else 0.0
    }

}*/
